query recentEntrypoints($projectId: ID!, $branch: String, $artifactName: String, $pagination: PaginationInput) {
  project(id: $projectId) {
    entrypointNames(branch: $branch, artifactName: $artifactName, pagination: $pagination)
  }
}
