query projectAuthedUsers($projectId: ID!) {
  project(id: $projectId) {
    owners {
      username
      avatarUrl
      email
    }
    viewers {
      username
      avatarUrl
      email
    }
  }
}