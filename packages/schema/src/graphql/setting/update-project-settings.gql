mutation updateProjectSettings($projectId: ID!, $settingsInput: UpdateSettingInput!) {
  updateProjectSettings(projectId: $projectId, input: $settingsInput) {
    labMessageSource
    bundleMessageFilter
    bundleMessageSource
    bundleMessageBranches
    autoDetectVersion
    messageTargetType
    messageTarget {
      userEmails
    }
  }
}