query basicSettings($projectId: ID!) {
  project(id: $projectId) {
    setting {
      labMessageSource
      bundleMessageFilter
      bundleMessageSource
      bundleMessageBranches
      messageTargetType
      autoDetectVersion
      messageTarget {
        userEmails
      }
    }
  }
}