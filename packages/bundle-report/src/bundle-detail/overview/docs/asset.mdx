## Webpack ouput assets

Files emitted by `webpack`.

Including any `JavaScript` `CSS` `font` `Image` files which processed by `webpack`.
<br />
Files copied by webpack plugins like `CopyWebpackPlugin` but not processed by `loader` also occurrent in `assets`.
