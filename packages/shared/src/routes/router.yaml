base: /
paths:
  docs:
    base: /docs
    paths:
      api: /api
  features:
    base: /features
    paths:
      bundle: /bundle
      lab: /lab
      source: /source
  projects: /projects
  notFound: /404
  status: /status
  license: /license
  applications: /applications
  editPassword: /edit-password
  resetPassword: /reset-password
  me:
    base: /me
    paths:
      connectedAccounts: /connected-accounts
      billing: /billing
      accessToken: /access-token
  login: /login
  importGithub: /import/github
  register: /register
  project:
    base: /projects/:projectId
    paths:
      feature: /:feature?
      home: /home
      statistics:
        base: /statistics
        paths:
          artifacts: /artifacts
          snapshots: /snapshots
      bundle:
        base: /bundle
        paths:
          detail: /:bundleId
          jobBundleContent: /:bundleId/bundle-content
      lab:
        base: /lab
        paths:
          report: /reports/:reportId/:tabName?
      competitor:
        base: /competitor
        paths:
          report: /reports/:tabName
      source: /source
      report: /report
      settings: /settings/:settingName?
      jobTrace: /jobs/:type/:entityId
